# Run the Discord bot on Replit

1. Add the Discord bot token as the Replit Secret `DISCORD_BOT_TOKEN`.
2. Start the `Discord bot` workflow.
3. The bot runs with `python bot.py` and provides the global `/ping` command.

The `/ping` slash command may take a little while to appear in Discord after the
bot starts because commands are synced globally.